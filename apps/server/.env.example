# Server configuration (copy to .env for local overrides)

# Port the Socket.IO/HTTP server listens on.
PORT=4000

# Comma-separated list of allowed browser origins, or "*" for any (LAN play).
CLIENT_ORIGIN=http://localhost:3000
