# Use a flat (hoisted) node_modules layout. The three.js / react-three ecosystem
# relies on many transitive peers; hoisting avoids phantom-dependency friction and
# keeps Next.js + R3F resolution behaving like a classic npm install.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
